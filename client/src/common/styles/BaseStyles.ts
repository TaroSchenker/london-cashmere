import { css } from "@emotion/react";

// Base styles for the application. Adjust as needed.
const baseStyles = css`
  /* Currently using Tailwind's base styles. Additional styles can be added here. */
`;

export default baseStyles;
